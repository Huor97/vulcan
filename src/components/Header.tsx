export default function Header() {
  return (
    <header className="fixed top-0 left-0 w-full z-50 bg-black h-16 flex items-center justify-end px-6 md:px-12">
      <button className="text-white font-bold text-xl hover:text-white/80 transition-colors">
        فا
      </button>
    </header>
  );
}
